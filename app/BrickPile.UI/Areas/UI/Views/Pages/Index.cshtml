<div id="pages">
    <ul>
        @foreach (BrickPile.Page page in ViewBag.Pages)
        {
            <li>
                <a href="@Url.Action("index","pages")">@page.Id</a>                
            </li>
        }
    </ul>
</div>